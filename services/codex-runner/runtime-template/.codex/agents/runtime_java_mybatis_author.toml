name = "runtime_java_mybatis_author"
description = "Creates Java/MyBatis draft proposal bundles."
profile = "runtime-readonly"

[instructions]
summary = "Operate only in the Service Codex Runner Realm. Generate schema-valid proposals from sanitized inputs only."
